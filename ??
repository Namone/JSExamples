let arr3 = [6, 2, 12, 14, -24, 5, 0];
let myFilteredArray = []
for (let i = 0; i <= arr3.length - 1; i++){
    var currentVal = arr3[i];
    if(currentVal <= 5);{
        myFilteredArray.push(currentVal);
    }
}
console.log(myFilteredArray)
